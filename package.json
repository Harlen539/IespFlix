{
  "name": "iespflix",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix front-end && npm install --prefix back-end",
    "dev": "concurrently \"npm run dev --prefix back-end\" \"npm run dev --prefix front-end\""
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
